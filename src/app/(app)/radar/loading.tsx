import { SkeletonPanelPage } from "@/components/ui/primitives";

/** radar is a stack of panels, not a table and not a tile grid. */
export default function Loading() {
  return <SkeletonPanelPage />;
}
